# simple-token-dex
A lightweight decentralized exchange enabling instant swaps between two ERC-20 tokens using the Constant Product Formula (x * y = k). Includes liquidity addition/removal and price calculation logic.
